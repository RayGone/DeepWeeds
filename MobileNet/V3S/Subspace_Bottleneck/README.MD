# Subspace Fully Connected Layer
> A parameter efficient dense localized feature expansion layer.

Idea is to divide the feature vector to multiple spaces. Then apply a Fully Connected Layer to each spaces.

## Architecture
